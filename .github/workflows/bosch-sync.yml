name: Bosch Stock Export Sync

on:
  schedule:
    - cron: '0 5 * * *'   # 05:00 UTC = 07:00 SAST, every day
  workflow_dispatch:        # lets you also trigger it manually from the Actions tab

jobs:
  fetch-export:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Download Bosch Material Export (diagnostic)
        run: |
          HTTP_CODE=$(curl -sSL -o bosch_stock_export.csv -w "%{http_code}" \
            -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0 Safari/537.36" \
            "https://extranet-emea.bosch-pt.com/cgi-bin/WebObjects.dll/V5Prod_CatalogWeb.woa/wa/Exchange/material?exchange=nbukp1kqrcojsg35nj3pedv872&customer=9781000885")
          echo "HTTP status code: $HTTP_CODE"
          echo "----- Response content (first 40 lines) -----"
          head -40 bosch_stock_export.csv
          echo "-----------------------------------------------"
          if [ "$HTTP_CODE" != "200" ]; then
            echo "Non-200 response — stopping here, this run should show as failed."
            exit 1
          fi

      - name: Verify the download isn't empty/broken
        run: |
          if [ ! -s bosch_stock_export.csv ]; then
            echo "Download failed or returned an empty file"
            exit 1
          fi
          echo "First few lines:"
          head -5 bosch_stock_export.csv

      - name: Commit and push if the file changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add bosch_stock_export.csv
          git diff --staged --quiet || git commit -m "Update Bosch stock export $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          git push
